#pragma once
#ifndef SOURCE_PLAYER_EDIT_VXL_HDR
#define SOURCE_PLAYER_EDIT_VXL_HDR
/* Player world edit functions. */

#include "directives/dextern.h"

/* Breaks or places the current block and modifies inventory to reflect the change. */
VX_C_FUNC void vxplr_edit_general(int button, int action);

#endif
